<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna background -->
    <solid android:color="@color/white" />

    <!-- Radius sudut -->
    <corners android:radius="16dp" />

    <!-- Tambahkan border (opsional) -->
    <stroke
        android:width="2dp"/>

</shape>
